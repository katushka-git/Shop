@model MyReklama.Models.IndexViewModel2
@using MyReklama.Helpers



<!DOCTYPE html>

<html>
<h3>Наши услуги</h3>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Добавить новую", "Create")
    </p>
    @*<form method="get">
            <div class="form-inline">
                <label class="control-label">Услуга: </label>
                @Html.DropDownList("name",  as SelectList,
                            htmlAttributes: new { @class = "form-control" })

                <input type="submit" value="Фильтр" class="btn btn-default" />
            </div>
        </form>*@
    <input type="submit" value="Фильтр по цене" onclick="location.href='@Url.Action("SortName", "Service")'"class="btn btn-default" />
    <table class="table">
        <tr>
            <th>
                Наименование услуги
            </th>
            <th>
                Цена
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model.Service)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.ActionLink("Изменить", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Подробнее", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Удалить", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
    <div class="btn-group">
        @Html.PageLinks(Model.PageInfo, x => Url.Action("Index", new { page = x }))
    </div>
</body>
</html>
